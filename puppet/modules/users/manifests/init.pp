class users {

    ####################################
    # Human Users: Keep sorted by uid! #
    ####################################

    adduser { "mckeimic": id => 1001, groups => ["admin"]}
    adduser { "mah":      id => 1002, groups => ["admin"]}
}
